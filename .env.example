# 单镜像内 MySQL 时由入口脚本设为 127.0.0.1；仅外部 MySQL 时需改
DB_HOST=127.0.0.1
DB_PORT=3306
DB_USER=relay
DB_PASSWORD=relaypass
DB_NAME=openwechat-claw
DB_ROOT_PASSWORD=rootpass
APP_PORT=8000
# 可选：注册人数上限，达到后返回 503，避免无限刷号（不设则不限制）
# MAX_USERS=5000
